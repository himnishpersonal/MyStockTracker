<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<title>MyStockTracker</title>

  <style>
    html {
      height: 100%;
      background-color: #092635;
    }

    body {
      margin:0;
      padding:0;
      font-family: "JetBrains Mono",monospace;
      background: linear-gradient(#141e30, #243b55);
    }

    .box {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 400px;
      padding: 40px;
      transform: translate(-50%, -50%);
      background: rgba(0,0,0,.5);
      box-sizing: border-box;
      box-shadow: 0 15px 25px rgba(0,0,0,.6);
      border-radius: 10px;
    }

    .box h2 {
      margin: 0 0 30px;
      padding: 0;
      color: #fff;
      text-align: center;
    }

    .box .box2 {
      position: relative;
    }

    .box .box2 input {
      width: 100%;
      padding: 10px 0;
      font-size: 16px;
      color: #fff;
      margin-bottom: 30px;
      border: none;
      border-bottom: 1px solid #fff;
      outline: none;
      background: transparent;
    }

    .box .box2 label {
      position: absolute;
      top:0;
      left: 0;
      padding: 10px 0;
      font-size: 16px;
      color: #fff;
      pointer-events: none;
      transition: .5s;
    }

    .box .box2 input:focus ~ label,
    .box .box2 input:valid ~ label {
      top: -25px;
      left: 0;
      color: #9EC8B9;
      font-size: 12px;
    }

    .box form a {
      position: relative;
      display: inline-block;
      padding: 10px 20px;
      color: #9EC8B9;
      font-size: 16px;
      text-decoration: none;
      text-transform: uppercase;
      overflow: hidden;
      transition: .5s;
      margin-top: 40px;
      letter-spacing: 4px
    }

    .box a:hover {
      background: #9EC8B9;
      color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 5px #9EC8B9,
                  0 0 25px #9EC8B9,
                  0 0 50px #9EC8B9,
                  0 0 100px #9EC8B9;
    }

    .box a span {
      position: absolute;
      display: block;
    }

    .box a span:nth-child(1) {
      top: 0;
      left: -100%;
      width: 100%;
      height: 2px;
      background: linear-gradient(90deg, transparent, #9EC8B9);
      animation: btn-anim1 1s linear infinite;
    }

    @keyframes btn-anim1 {
      0% {
        left: -100%;
      }
      50%,100% {
        left: 100%;
      }
    }

    .box a span:nth-child(2) {
      top: -100%;
      right: 0;
      width: 2px;
      height: 100%;
      background: linear-gradient(180deg, transparent, #9EC8B9);
      animation: btn-anim2 1s linear infinite;
      animation-delay: .25s
    }

    @keyframes btn-anim2 {
      0% {
        top: -100%;
      }
      50%,100% {
        top: 100%;
      }
    }

    .box a span:nth-child(3) {
      bottom: 0;
      right: -100%;
      width: 100%;
      height: 2px;
      background: linear-gradient(270deg, transparent, #9EC8B9);
      animation: btn-anim3 1s linear infinite;
      animation-delay: .5s
    }

    @keyframes btn-anim3 {
      0% {
        right: -100%;
      }
      50%,100% {
        right: 100%;
      }
    }

    .box a span:nth-child(4) {
      bottom: -100%;
      left: 0;
      width: 2px;
      height: 100%;
      background: linear-gradient(360deg, transparent, #9EC8B9);
      animation: btn-anim4 1s linear infinite;
      animation-delay: .75s
    }

    @keyframes btn-anim4 {
      0% {
        bottom: -100%;
      }
      50%,100% {
        bottom: 100%;
      }
    }

    button {
      align-items: center;
      border: 0;
      color: #fff;
      cursor: pointer;
  		display: inline-flex;
      background-color: transparent;
      font-family: "JetBrains Mono",monospace;
      font-size: small;
    }

    button:hover {
      color: #092635;
    }

    p {
      background-color: #092635;
      color:#fff;
      padding-top: 0.75rem;
    }

    p:hover {
      font-size: 1.05rem;

    }

    .back {
      font-size: 1rem;
      animation-delay: .25s
    }

    .back:hover {
      color: #9EC8B9;
      border-radius: 5px;
    }
  </style>
</head>

<body>
  <div>
    <p>&nbsp;&nbsp;&nbsp;<a href="/"><button class="back" type="submit">&#8592; Back</button></a></p>
  </div>

  <div class="box">
    <h2>Sign Up</h2>

    <form action="/signup" method="post">

    <div class="box2">
      <input type="text" name="username" required>
      <label>Username</label>
    </div>
    
    <div class="box2">
      <input type="password" name="password" required>
      <label>Password</label>
    </div>
    
    <a>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <button type="submit">Submit</button>
    </a>
    </form>
  </div>


</body>

</html>